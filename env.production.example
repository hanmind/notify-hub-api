# 배포 환경 설정 예시
# AWS Lambda 환경변수 설정 시 참고용

# 애플리케이션 설정
DEBUG=False
APP_NAME=Notification Service
APP_VERSION=1.0.0

# 환경 설정
ENVIRONMENT=production

# 스케줄러 설정 (배포환경에서는 비활성화)
# ENVIRONMENT=production이면 자동으로 스케줄러 비활성화
# AWS EventBridge가 스케줄링을 담당

# 데이터베이스 설정 (AWS RDS)
DB_HOST=your-rds-endpoint.amazonaws.com
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=notification_service

# NCloud SENS 설정
NCLOUD_ACCESS_KEY=your_ncloud_access_key
NCLOUD_SECRET_KEY=your_ncloud_secret_key
NCLOUD_SERVICE_ID=your_sens_service_id
NCLOUD_PHONE_NUMBER=01012345678

# 서비스별 발송자 이메일 설정
NCLOUD_SENDER_EMAIL_SERVICE_A=noreply@example.com
NCLOUD_SENDER_EMAIL_SERVICE_B=noreply@example.com
NCLOUD_SENDER_EMAIL_SERVICE_C=noreply@example.com
# 기본 발송자 (fallback)
NCLOUD_SENDER_EMAIL=noreply@example.com

# AWS 설정
AWS_REGION=ap-northeast-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# JWT 설정
SECRET_KEY=your-super-secret-jwt-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
